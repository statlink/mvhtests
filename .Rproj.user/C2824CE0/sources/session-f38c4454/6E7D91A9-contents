#useDynLib(fsn, .registration = TRUE)

importFrom("stats", pchisq, optim, rpois, pnorm, cor)
importFrom("Rfast", rowMins, colmeans, colsums, colVars)

exportPattern("^[[:alpha:]]+")
exportPattern("^[^\\.]")

export(bp.mle, rbp, bp.gof, bp.gof2, lambda3.profile)
